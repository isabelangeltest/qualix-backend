# backend-qualix/nixpacks.toml
[phases.setup]
nixPkgs = ["chromium"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[variables]
PORT = "8080"
PUPPETEER_EXECUTABLE_PATH = "/usr/bin/chromium"

[start]
cmd = "npm start"
